import 'package:flutter/material.dart';

class AppColors {
  // Paleta Industrial Red
  static const Color background = Color(0xFF121212); // Preto levemente mais suave
  static const Color surface = Color(0xFF1E1E1E);    // Cinza escuro para os cards
  static const Color primary = Color(0xFFB22222);    // Vermelho Industrial (Firebrick)
  static const Color accent = Color(0xFFF0F0F0);     // Branco Gelo para contrastes
  
  // Texto
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9E9E); // Cinza médio para legendas

  // Gamificação
  static const Color xpBar = Color(0xFFB22222);      // Barra de progresso vermelha
  static const Color success = Color(0xFF27AE60);    // Verde para confirmações
}